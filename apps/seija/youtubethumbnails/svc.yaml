apiVersion: v1
kind: Service
metadata:
  name: youtubethumbnails
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: youtubethumbnails
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
