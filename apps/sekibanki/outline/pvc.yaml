apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valkey-outline-pvc
  namespace: outline
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
