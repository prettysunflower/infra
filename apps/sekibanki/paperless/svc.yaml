apiVersion: v1
kind: Service
metadata:
  name: paperless
  namespace: paperless
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: paperless
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
