apiVersion: v1
kind: Service
metadata:
  name: deneb
  namespace: radarr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: radarr-deneb
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
