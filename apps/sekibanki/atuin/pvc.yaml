apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: atuin-config-pvc
  namespace: atuin
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: local-path
  resources:
    requests:
      storage: 128Mi
