---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50G
  storageClassName: nfs-csi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-config-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 64M
  storageClassName: nfs-csi