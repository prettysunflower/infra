apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: karakeep-pvc
  namespace: karakeep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: local-path
