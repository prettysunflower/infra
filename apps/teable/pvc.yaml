apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valkey-data-pvc
  namespace: teable
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: s3yuyuko
  resources:
    requests:
      storage: 2Gi