apiVersion: v1
kind: Service
metadata:
  name: nyallo
  namespace: redmine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nyallo
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
