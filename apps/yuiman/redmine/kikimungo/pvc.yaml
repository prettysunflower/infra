apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redmine-kikimungo-pvc
  namespace: redmine
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
  storageClassName: nfs-csi
