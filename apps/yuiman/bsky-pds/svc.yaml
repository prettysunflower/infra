apiVersion: v1
kind: Service
metadata:
  name: pds
  namespace: bsky-pds
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pds
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
