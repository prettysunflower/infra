apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pds-pvc
  namespace: bsky-pds
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 25Gi
  storageClassName: local-path
